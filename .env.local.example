# Frontend environment template — copy to .env.local for local dev.
NEXT_PUBLIC_API_URL=http://localhost:8000
# Set to "true" only once the backend has a real STT_PROVIDER/STT_API_KEY configured,
# so useSpeechRecognition's MediaRecorder fallback knows the server path exists.
NEXT_PUBLIC_SERVER_STT_ENABLED=false
